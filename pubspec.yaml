name: mood_tracker
description: A Flutter app to track user moods daily.
publish_to: "none" # isso evita publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: "^3.7.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.31.0
  firebase_auth: ^4.19.0
  cloud_firestore: ^4.17.3
  provider: ^6.1.2
  location: ^6.0.2
  http: ^1.2.1
  intl: 0.17.0 # APENAS UMA VEZ aqui!

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
